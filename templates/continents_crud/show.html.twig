{% extends 'base.html.twig' %}

{% block title %}Continents{% endblock %}

{% block body %}
    <h1>Continents</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ continent.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ continent.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_continents_crud_index') }}">back to list</a>

    <a href="{{ path('app_continents_crud_edit', {'id': continent.id}) }}">edit</a>

    {{ include('continents_crud/_delete_form.html.twig') }}
{% endblock %}
