{% extends 'base.html.twig' %}

{% block title %}Pays{% endblock %}

{% block body %}
    <h1>Pays</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pay.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pay.name }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ pay.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pays_controller2_index') }}">back to list</a>

    <a href="{{ path('app_pays_controller2_edit', {'id': pay.id}) }}">edit</a>

    {{ include('pays_controller2/_delete_form.html.twig') }}
{% endblock %}
