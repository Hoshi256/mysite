{% extends 'base.html.twig' %}

{% block title %}Hello BackOfficeUserController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>{% trans %}My list commands{% endtrans %}</h1>
    <table class="table">
     <thead>
    <tr>            
        <th width="200"><strong>{% trans %}Product{% endtrans %}</strong></th>
        <th width="200"><strong>{% trans %}DateIn{% endtrans %}</strong></th>
        <th width="200"><strong>{% trans %}DateOut{% endtrans %}</strong></th>
        <th width="200"><strong>{% trans %}Price{% endtrans %}</strong></th>
        <th width="200"><strong>{% trans %}Nb Persons{% endtrans %}</strong></th>
        <th width="200"><strong>{% trans %}Action{% endtrans %}</strong></th>


    </tr>
    </thead>
    <tbody>
        {# {% if history is not empty %} #}
            {% for booking in history %}

<tr>
 <th>{{booking.product.name}}</th>
        <td>{{ booking.dateIn|date('Y-m-d') }}</td>
        <td>{{ booking.dateOut|date('Y-m-d') }}</td>
        <td>{{ booking.price }}</td>
        <td>{{ booking.nbPerson }}</td>
        <td>
 <a href="{{ path('app_generate_invoice') }}?booking={{ booking.id }}">

 <button type="button" class="btn btn-primary btn-sm">
    <i class="bi bi-download"></i>{% trans %} Facture{% endtrans %}

  </button>
 
 </a> </td>
</tr>
       
        {% endfor %}
    {# {% else %} #}
  {# <p>No command history found.</p> #}
{# {% endif %} #}
 </tbody>
    </table>


  



{# <td>{{ user.Id}}</td> #}
{# <td>{{ history.dateIn }}</td> #}



{# <td>{{ history.product_id}}</td> #}




{# <td>{{ user.booking.product.name}}</td> #}


    {# {% for command in commands %} #} 
             {# <td>{{ booking.product.name }}</td>
            <td>{{ booking.dateIn ? booking.dateIn|date('Y-m-d H:i:s') : '' }}</td>
             <td>{{ booking.dateOut ? booking.dateOut|date('Y-m-d H:i:s') : '' }}</td>
            <td>{{ booking.price }}</td>
            <td>{{ booking.nbPerson }}</td> #}

{# {# {% endfor %} #}


   
    
</div>
{% endblock %}
