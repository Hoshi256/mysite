{% extends 'base.html.twig' %}

{% block title %}Vacance{% endblock %}

{% block body %}
    <h1>Vacance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vacance.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vacance.name }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ vacance.prix }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ vacance.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vacance_index') }}">back to list</a>

    <a href="{{ path('app_vacance_edit', {'id': vacance.id}) }}">edit</a>

    {{ include('vacance/_delete_form.html.twig') }}
{% endblock %}
