{% extends 'base.html.twig' %}

{% block title %}Countries{% endblock %}

{% block body %}
    <h1>Countries</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ country.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ country.name }}</td>
            </tr>
            <tr>
                <th>Population</th>
                <td>{{ country.population }}</td>
            </tr>
            <tr>
                <th>Capital</th>
                <td>{{ country.capital }}</td>
            </tr>
            <tr>
                <th>Monney</th>
                <td>{{ country.monney }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_countries_crud_index') }}">back to list</a>

    <a href="{{ path('app_countries_crud_edit', {'id': country.id}) }}">edit</a>

    {{ include('countries_crud/_delete_form.html.twig') }}
{% endblock %}
